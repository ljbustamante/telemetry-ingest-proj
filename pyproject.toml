
[tool.pytest.ini_options]
addopts = "-q --cov=src --cov-report=term-missing"
testpaths = ["tests"]
# "." permite `import src...` (handlers y tests) y `....infrastructure` desde `src.entrypoints.http.front`.
pythonpath = [".", "src"]
